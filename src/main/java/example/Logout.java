package example;

import org.apache.wicket.PageParameters;

/**
 * Logout just says goodbye. May be removed and replaced
 *  with a redirect->HomePage if that looks nicer.
 */
public class Logout extends Base {
    public Logout(final PageParameters parameters) {
        super(parameters);
    }
}
